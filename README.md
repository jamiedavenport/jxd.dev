# JXD

Personal website of Jamie Davenport, deployed at [jxd.dev](https://jxd.dev).

Stack: TanStack Start (Vite + Nitro + React 19 + Tailwind v4).

```bash
pnpm install
pnpm dev      # http://localhost:3000
pnpm build
pnpm preview
```
